<ctrl:FrostyDockableWindow x:Class="ClothDataPlugin.Windows.ClothDataGeneratorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
        xmlns:core="clr-namespace:Frosty.Core.Controls;assembly=FrostyCore"
        mc:Ignorable="d"
        Title="Cloth Data Generator" Height="520" Width="580" 
        ResizeMode="NoResize" 
        WindowStartupLocation="CenterScreen"
        FrostyLoaded="FrostyDockableWindow_FrostyLoaded">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="38"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="{StaticResource ListBackground}">
            <StackPanel Margin="10">
                
                <!-- Target Mesh Section (the mesh we're adding cloth TO) -->
                <TextBlock Text="Target Mesh (adding cloth to)" FontWeight="Bold" Foreground="{StaticResource FontColor}" Margin="0,0,0,5"/>
                <DockPanel Margin="5,0,5,5" LastChildFill="True">
                    <Label Content="Asset:" Width="110" Foreground="{StaticResource FontColor}" FontFamily="Global User Interface"/>
                    <TextBox x:Name="AssetPathText" Padding="4" FontFamily="Global User Interface" BorderThickness="1" IsEnabled="False"/>
                </DockPanel>
                <DockPanel Margin="5,0,5,10" LastChildFill="True">
                    <Label Content="Mesh Info:" Width="110" Foreground="{StaticResource FontColor}" FontFamily="Global User Interface"/>
                    <TextBox x:Name="MeshInfoText" Padding="4" FontFamily="Global User Interface" BorderThickness="1" IsEnabled="False"/>
                </DockPanel>
                
                <Separator Margin="0,5,0,10" Background="{StaticResource ControlBackground}"/>
                
                <!-- Template Mesh Section (the mesh with cloth we're copying FROM) -->
                <TextBlock Text="Template Mesh (copy cloth from)" FontWeight="Bold" Foreground="#FFA500" Margin="0,0,0,3"/>
                <TextBlock Text="Select a mesh that has cloth data you want to copy" 
                           FontSize="11" Foreground="{StaticResource FontColor}" Opacity="0.7" Margin="0,0,0,8"/>
                
                <DockPanel Margin="5,0,5,5" LastChildFill="True">
                    <Button Content="Browse Mesh..." Width="100" DockPanel.Dock="Right" Margin="5,0,0,0" Click="BrowseTemplateMesh_Click"/>
                    <Label Content="ClothWrapping:" Width="110" Foreground="{StaticResource FontColor}" FontFamily="Global User Interface"/>
                    <TextBox x:Name="TemplateClothWrappingText" Padding="4" FontFamily="Global User Interface" BorderThickness="1" IsEnabled="False" Text="(Select template mesh)"/>
                </DockPanel>
                <DockPanel Margin="5,0,5,10" LastChildFill="True">
                    <Label Width="100" DockPanel.Dock="Right" Margin="5,0,0,0"/>
                    <Label Content="EACloth:" Width="110" Foreground="{StaticResource FontColor}" FontFamily="Global User Interface"/>
                    <TextBox x:Name="TemplateEAClothText" Padding="4" FontFamily="Global User Interface" BorderThickness="1" IsEnabled="False" Text="(Select template mesh)"/>
                </DockPanel>
                
                <Separator Margin="0,5,0,10" Background="{StaticResource ControlBackground}"/>
                
                <!-- Target Resources Section (auto-detected or manual) -->
                <TextBlock Text="Target Resources (to replace)" FontWeight="Bold" Foreground="{StaticResource FontColor}" Margin="0,0,0,3"/>
                <TextBlock Text="Auto-detected from target mesh. Clear to create new resources." 
                           FontSize="11" Foreground="{StaticResource FontColor}" Opacity="0.7" Margin="0,0,0,8"/>
                
                <DockPanel Margin="5,0,5,5" LastChildFill="True">
                    <Button Content="Clear" Width="50" DockPanel.Dock="Right" Margin="5,0,0,0" Click="ClearTargetClothWrapping_Click"/>
                    <Button Content="Browse..." Width="75" DockPanel.Dock="Right" Margin="5,0,0,0" Click="BrowseTargetClothWrapping_Click"/>
                    <Label Content="ClothWrapping:" Width="110" Foreground="{StaticResource FontColor}" FontFamily="Global User Interface"/>
                    <TextBox x:Name="TargetClothWrappingText" Padding="4" FontFamily="Global User Interface" BorderThickness="1" IsEnabled="False" Text="(Will create new)"/>
                </DockPanel>
                <DockPanel Margin="5,0,5,10" LastChildFill="True">
                    <Button Content="Clear" Width="50" DockPanel.Dock="Right" Margin="5,0,0,0" Click="ClearTargetEACloth_Click"/>
                    <Button Content="Browse..." Width="75" DockPanel.Dock="Right" Margin="5,0,0,0" Click="BrowseTargetEACloth_Click"/>
                    <Label Content="EACloth:" Width="110" Foreground="{StaticResource FontColor}" FontFamily="Global User Interface"/>
                    <TextBox x:Name="TargetEAClothText" Padding="4" FontFamily="Global User Interface" BorderThickness="1" IsEnabled="False" Text="(Will create new)"/>
                </DockPanel>
                
                <Separator Margin="0,5,0,10" Background="{StaticResource ControlBackground}"/>
                
                <!-- New Resource Name -->
                <DockPanel Margin="5,0,5,5" LastChildFill="True">
                    <Label Content="New name prefix:" Width="110" Foreground="{StaticResource FontColor}" FontFamily="Global User Interface"/>
                    <TextBox x:Name="NewResourceNameText" Padding="4" FontFamily="Global User Interface" BorderThickness="1"/>
                </DockPanel>
                
                <!-- Status Bar -->
                <Border Margin="0,10,0,0" Background="{StaticResource ControlBackground}" CornerRadius="2" Padding="8,5">
                    <TextBlock x:Name="StatusText" Text="Select target mesh, then choose template mesh" FontSize="11" Foreground="{StaticResource FontColor}"/>
                </Border>
                
            </StackPanel>
        </Grid>

        <!-- Bottom Button Bar -->
        <Border Grid.Row="1" Background="{StaticResource WindowBackground}">
            <Grid Margin="8">
                <Button x:Name="cancelButton" Content="Cancel" HorizontalAlignment="Left" Width="75" Click="CancelButton_Click"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="generateButton" Content="Generate" Width="100" Click="GenerateButton_Click" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</ctrl:FrostyDockableWindow>
